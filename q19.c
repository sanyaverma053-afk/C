
//Q8.Write a program to check whether a number is zero or non-zero.

#include<stdio.h>
int main()
{
    int a;
    printf("enter a number");
    scanf("%d",&a);
    if(a==0)
    {
        printf("a is zero");
    }
    else
    {
        
printf("a is non zero");
    }
    return 0;
}
